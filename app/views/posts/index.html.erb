<%= render 'form' %>

<h1>Posts#index</h1>
<table>
    <% @posts.each do |post| %>
    <tr>
        <td><%= post.body %></td>
        <td><%= link_to 'destroy', post, method: :delete %></td>
    </tr>
    <% end %>
</table>